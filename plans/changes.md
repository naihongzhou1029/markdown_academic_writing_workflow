# 2025/12/04

- **Add commit log sync requirement to AGENTS.md**
  - Add new principle requiring agents to sync commit logs to `plans/changes.md` before presenting commit messages
  - Specify day-based timestamp format (`# YYYY/MM/DD`) for organizing commit logs
  - Require semantic merging of multiple commits on the same day into a single date section

